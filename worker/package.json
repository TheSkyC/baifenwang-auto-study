{
  "name": "bfw-update-api",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo \"No build step\"",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
